# libft
Libft is a custom C standard library implemented as part of the 42 curriculum. It includes re-coded libc functions, additional utility functions for strings, memory, linked lists, various algorithmic helpers and all written from scratch following strict norms.
